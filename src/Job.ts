/** 
* an attempt to unify the jobs and the job roll enums into one
* */
export enum Job {
    PLD = 1, WAR, DRK, GNB,
    WHM, SCH, AST,
    MNK, DRG, NIN, SAM,
    BRD, MCH, DNC,
    BLM, SMN, RDM
}